// Operations Order

let result;

result = (20 + 30) * 2;

result = (100 + 200 + 300) * .2;

console.log(result)

// Increments

let score = 10;

score++; // Increments in +1
++score;

score--; // Decrements in -1
--score;

score += 10; // Increments in +10

console.log(score);