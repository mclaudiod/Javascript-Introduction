// Difference between functions and methods

const number1 = 20;
const number2 = "20";

console.log(parseInt(number2)); // parseInt() is a function that changes the type of data to number if valid
console.log(number1.toString()); // toString() is a method that changes the type of data to string